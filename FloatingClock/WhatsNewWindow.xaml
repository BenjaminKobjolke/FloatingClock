<Window x:Class="FloatingClock.WhatsNewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="What's New"
        Width="450"
        Height="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F0F0F0">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="VersionText" FontSize="16" FontWeight="Bold" Foreground="#333333"/>
        <TextBlock x:Name="TitleText" Grid.Row="1" FontSize="13" Margin="0,5,0,10" TextWrapping="Wrap" Foreground="#333333"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="NotesList" Foreground="#333333">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StringFormat=• {0}}" TextWrapping="Wrap" Margin="0,0,0,6"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="OlderButton" MinWidth="70" Margin="0,0,10,0" Padding="10,4" Click="OlderButton_Click"/>
            <Button x:Name="NewerButton" MinWidth="70" Margin="0,0,10,0" Padding="10,4" Click="NewerButton_Click"/>
            <Button x:Name="CloseButton" MinWidth="70" Padding="10,4" Click="CloseButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
